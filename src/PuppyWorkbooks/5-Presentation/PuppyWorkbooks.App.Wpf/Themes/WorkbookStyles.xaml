<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:PuppyWorkbooks.ViewModels;assembly=PuppyWorkbooks.ViewModels"
                    xmlns:views="clr-namespace:PuppyWorkbooks.App.Wpf.Views;assembly=PuppyWorkbooks.App.Wpf">

    <!-- Colors / brushes -->
    <SolidColorBrush x:Key="ShellBackgroundBrush" Color="#E7F0FB" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="HeaderForegroundBrush" Color="#222222" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#6F6F6F" />
    <SolidColorBrush x:Key="ResultTextBrush" Color="#222222" />
    <SolidColorBrush x:Key="AccentBrush" Color="#2563EB" />

    <!-- Top-level layout margin -->
    <Thickness x:Key="PageMargin">32</Thickness>

    <!-- Card style (row container) -->
    <Style x:Key="FormulaCardStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="14" />
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
        <Setter Property="Padding" Value="16" />
        <Setter Property="Margin" Value="0,0,0,12" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#33000000" BlurRadius="12" ShadowDepth="2" Opacity="0.4" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Text styles -->
    <Style x:Key="FormulaNameTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource HeaderForegroundBrush}" />
    </Style>

    <Style x:Key="FormulaTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
    </Style>

    <Style x:Key="CommentsTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="ResultTextStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource ResultTextBrush}" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Add (+) button -->
    <Style x:Key="AddButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Content" Value="+" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="16">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <DropShadowEffect x:Key="PanelShadowEffect"
                      Color="#33000000"
                      BlurRadius="16"
                      ShadowDepth="2"
                      Opacity="0.3" />

    <!-- Converter instance (ensure the clr-namespace matches your converter class) -->
    <views:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />

    <!-- Sample data for design-time can be kept out of the global dictionary if undesired at runtime -->
</ResourceDictionary>